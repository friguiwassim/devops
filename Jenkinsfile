pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Clonage du dépôt réussi'
            }
        }

        stage('Build') {
            steps {
                echo 'Build en cours...'
            }
        }
    }
}
